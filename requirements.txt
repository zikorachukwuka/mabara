# Core
claude-agent-sdk==0.2.110   # Claude brain (needs Claude Code CLI installed + logged in)
sounddevice==0.5.5          # mic + speaker streams
soundfile==0.14.0
numpy==2.4.6
keyboard==0.13.5            # global push-to-talk hotkey

# Ears (speech-to-text)
onnx-asr[cpu,hub]==0.11.0   # nvidia parakeet-tdt-0.6b-v2 (default)
faster-whisper==1.2.1       # whisper fallback (--stt small.en)

# Mouth (text-to-speech)
piper-tts==1.4.2            # snappy default voice (--tts piper)
supertonic==1.3.1           # natural-voice option (--tts supertonic)
kokoro-onnx==0.5.0          # quality reference (--tts kokoro)
misaki[en]==0.9.4           # proper G2P phonemization for kokoro
onnxruntime==1.27.0
